services:
  manager:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["dev"]
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      NODE_ENV: development
      LOG_LEVEL: 2
    restart: unless-stopped

